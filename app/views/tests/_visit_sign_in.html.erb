<%= "#{session[:email]} visited" %>
<% if session[:email] %>
  <script>
    _kmq.push(['identify', '<%= session[:email] %>']);

    _kmq.push(function() {
      alert(KM.i() + "is the kissmetric identity"); // Display an alert box with your current KM identity
    });
    //_kmq.push(['alias', 'luna@bitcanny.com']);
    //_kmq.push(['identify', 'luna@bitcanny.com']);
    _kmq.push(['record', 'site-visited']);
  </script>
<% end %>
<script>
  function getCookie(cname) {
    var name = cname + "=";
    var ca = document.cookie.split(';');
    for(var i = 0; i <ca.length; i++) {
      var c = ca[i];
      while (c.charAt(0)==' ') {
        c = c.substring(1);
      }
      if (c.indexOf(name) == 0) {
        return c.substring(name.length,c.length);
      }
    }
    return "";
  }
    //console.log(getCookie('km_ai').indexOf(encodeURIComponent('anonymous')));
</script>